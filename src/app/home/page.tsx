import HomeComponent from '@/src/components/home/HomeComponent';

export default () => (
  <main className='relative flex h-screen w-full flex-col items-center justify-start overflow-hidden bg-black md:hidden'>
    <HomeComponent />
  </main>
);
