import HomeComponent from '@/src/components/home/Home';

export default () => (
  <section className='relative flex h-screen w-full flex-col items-center justify-start overflow-hidden bg-black md:hidden'>
    <HomeComponent />
  </section>
);
